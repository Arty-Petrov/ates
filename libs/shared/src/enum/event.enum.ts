export enum Event {
  AccountRegistered = 'account.user.registered',
  AccountSignIn = 'account.auth.sign-in',
  TaskAssigned = 'tracker.task-flow.assigned',
  TaskCreated = 'tracker.task-flow.created',
  TaskCompleted = 'tracker.task-flow.completed',
}
